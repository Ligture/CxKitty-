# CxKitty Web UI

现代化网页界面，提供与原TUI相同的所有功能，采用美观实用的设计理念。

## ✨ 特性

### 现代化设计
- 🎨 采用渐变配色和流畅动画效果
- 📱 响应式设计，支持移动端和桌面端
- 🌈 直观的用户界面，易于操作
- ⚡ 实时任务进度展示

### 完整功能支持
- ✅ 多种登录方式（手机号密码、二维码、会话恢复）
- ✅ **多用户管理**（同时登录多个账号，随时切换）
- ✅ 课程列表展示和选择
- ✅ 自动完成视频、文档、作业任务点
- ✅ 实时任务执行日志
- ✅ 可配置的任务参数
- ✅ WebSocket实时通信

## 🚀 使用方法

### 方法一：直接运行

#### 1. 安装依赖

```bash
# 使用 poetry 安装
poetry install

# 或使用 pip 安装
pip install flask flask-socketio pillow \
    requests qrcode pyyaml beautifulsoup4 lxml \
    pycryptodome yarl dataclasses-json jsonpath-python \
    opencv-python numpy ddddocr rich openai
```

#### 2. 启动 Web UI

```bash
# 使用启动脚本（推荐）
./run_webui.sh

# 使用 poetry
poetry run python3 webui.py

# 或直接运行
python3 webui.py
```

#### 3. 访问界面

在浏览器中打开：http://127.0.0.1:5000

### 方法二：使用 Docker

#### 1. 构建并启动

```bash
# 使用 docker-compose（推荐）
docker-compose -f docker-compose.webui.yml up -d

# 或手动构建
docker build -f Dockerfile.webui -t cxkitty-webui .
docker run -d \
  --name cxkitty-webui \
  -p 5000:5000 \
  -v "$PWD/session:/app/session" \
  -v "$PWD/logs:/app/logs" \
  -v "$PWD/export:/app/export" \
  -v "$PWD/faces:/app/faces" \
  -v "$PWD/config.yml:/app/config.yml:ro" \
  cxkitty-webui
```

#### 2. 访问界面

在浏览器中打开：http://服务器IP:5000

#### 3. 查看日志

```bash
docker logs -f cxkitty-webui
```

#### 4. 停止服务

```bash
docker-compose -f docker-compose.webui.yml down
```

## 📖 使用说明

### 登录

Web UI 提供三种登录方式：

1. **密码登录**
   - 输入手机号和密码
   - 点击"登录"按钮

2. **扫码登录**
   - 点击"生成二维码"
   - 使用学习通客户端扫描
   - 确认登录

3. **会话登录**
   - 从已保存的会话列表中选择
   - 快速恢复登录状态

### 多用户管理 🆕

**新功能！** Web UI 现在支持在同一个页面管理多个账号：

1. **登录多个账号**
   - 可以通过任意登录方式添加多个账号
   - 已登录的账号会显示在用户切换器中
   - 用户切换器显示已登录账号的数量

2. **切换账号**
   - 点击顶部的用户切换器按钮
   - 从下拉列表中选择要切换的账号
   - 切换后会自动刷新该用户的课程列表

3. **退出账号**
   - 在用户切换器中，每个账号右侧都有退出按钮（×）
   - 点击退出按钮可以单独退出某个账号
   - 也可以点击"退出"按钮退出当前活跃的账号

4. **添加新账号**
   - 在用户切换器底部点击"登录其他用户"
   - 或者直接切换到登录页面进行新用户登录

### 选择课程

1. 登录成功后自动跳转到课程页面
2. 当有多个用户登录时，课程页面会显示当前用户
3. 点击课程卡片进行选择（可多选）
4. 选中的课程会高亮显示
5. 切换用户后，选中状态会被清空

### 执行任务

1. 选择要完成的课程
2. 点击"开始任务"按钮
3. 自动跳转到任务页面
4. 实时查看任务执行进度和日志

### 配置设置

在设置页面可以调整：
- 视频任务开关和等待时间
- 作业任务开关和等待时间
- 文档任务开关和等待时间
- 考试相关设置

## 🎯 界面预览与功能演示

### 登录页面
**设计理念：简洁、优雅、易用**

- ✨ 渐变紫色背景，视觉冲击力强
- 🎴 圆角卡片设计，现代感十足
- 📑 标签式切换，三种登录方式一目了然
- 🔄 实时二维码生成和状态更新
- 📱 支持移动端响应式布局

**功能特性：**
- 密码登录：实时表单验证
- 二维码登录：自动轮询状态，显示扫描人昵称
- 会话登录：卡片式会话列表，一键恢复

### 课程页面
**设计理念：清晰、直观、高效**

- 📚 网格式课程卡片布局，最多3列自适应
- 🖼️ 课程封面图片展示
- 👨‍🏫 显示教师、课程ID等详细信息
- 🏷️ 彩色状态标签（进行中/已完成）
- ✅ 点击卡片多选，选中高亮显示
- 🎨 鼠标悬停动画效果

**交互体验：**
- 流畅的卡片悬停效果
- 选中状态即时反馈
- 响应式网格布局

### 任务页面
**设计理念：实时、详细、专业**

- 📊 动态渐变进度条，实时更新
- 📝 彩色分类日志（信息/成功/警告/错误）
- 🖥️ 终端风格日志窗口（黑色背景）
- 📜 自动滚动到最新日志
- ⏱️ 每条日志带时间戳
- 🎯 清晰的任务状态标识（✓/✗/⚠）

**日志特性：**
- 蓝色：信息日志（章节开始、任务点处理）
- 绿色：成功日志（任务完成）
- 黄色：警告日志（章节未开放）
- 红色：错误日志（异常情况）

### 设置页面
**设计理念：分类清晰、配置简单**

- 🗂️ 分类配置面板（视频/作业/文档/考试）
- ⚙️ 开关式启用控制
- 🔢 数值输入框（等待时间）
- 💾 一键保存配置
- 📋 配置项说明清晰

**配置项：**
- 各任务类型的启用开关
- 等待时间配置
- Fuzzer功能开关
- 考试确认选项

## 🔧 技术栈

### 后端
- Flask - 轻量级 Web 框架
- Flask-SocketIO - WebSocket 实时通信
- 原有的 cxapi 模块

### 前端
- 原生 JavaScript (ES6+)
- Socket.IO 客户端
- CSS3 动画和渐变

## 📝 与原TUI的对比

| 功能 | TUI | Web UI |
|-----|-----|--------|
| 运行方式 | 终端 | 浏览器 |
| 界面交互 | 命令行 | 图形界面 |
| 多任务 | 顺序执行 | 后台线程 |
| 实时反馈 | 终端输出 | WebSocket推送 |
| 易用性 | 需要一定技术基础 | 零门槛使用 |
| 美观度 | 基础文本 | 现代化设计 |
| 远程访问 | 需要SSH | 直接浏览器 |
| 多用户 | ❌ | ✅（多浏览器） |
| 移动端 | ❌ | ✅（响应式） |
| 学习曲线 | 中等 | 极低 |

### 功能完整性对比

| 功能模块 | TUI | Web UI | 说明 |
|---------|-----|--------|------|
| 手机号密码登录 | ✅ | ✅ | 完全支持 |
| 二维码登录 | ✅ | ✅ | 完全支持 |
| 会话管理 | ✅ | ✅ | 完全支持 |
| 课程选择 | ✅ | ✅ | Web UI 支持可视化多选 |
| 视频任务 | ✅ | ✅ | 完全支持 |
| 文档任务 | ✅ | ✅ | 完全支持 |
| 作业任务 | ✅ | ✅ | 完全支持 |
| 考试模式 | ✅ | ✅ | 完全支持 |
| 验证码识别 | ✅ | ✅ | 自动处理 |
| 人脸识别 | ✅ | ✅ | 自动处理 |
| 配置管理 | ✅ | ✅ | Web UI 提供可视化界面 |
| 日志查看 | ✅ | ✅ | Web UI 提供彩色实时日志 |
| 进度显示 | ✅ | ✅ | Web UI 提供进度条 |

## 🚦 快速开始

以下是最简单的使用流程：

1. **安装依赖并启动**
   ```bash
   pip install flask flask-socketio pillow
   python3 webui.py
   ```

2. **浏览器访问** http://127.0.0.1:5000

3. **登录账号**（选择任一方式）
   - 手机号+密码
   - 扫码登录
   - 已保存的会话

4. **选择课程**
   - 点击课程卡片进行选择
   - 可以多选

5. **开始任务**
   - 点击"开始任务"按钮
   - 实时查看执行日志

## 🔧 故障排除

### WebSocket 连接失败

如果看到 "Socket.IO 未加载" 错误：

```bash
# 检查是否被防火墙拦截
# 或者在内网环境下使用本地IP访问
```

### 任务执行失败

1. 检查 `config.yml` 配置是否正确
2. 确保题库配置可用（如使用题库功能）
3. 查看浏览器控制台和服务器日志

### 页面无响应

1. 刷新浏览器页面
2. 检查服务器是否正常运行
3. 查看服务器日志输出

### 登录失败

1. 检查手机号和密码是否正确
2. 尝试使用二维码登录
3. 清除浏览器缓存和 Cookie

## ⚠️ 注意事项

1. Web UI 与原 TUI 功能完全一致，可以互相替代使用
2. 首次使用需要安装额外的 Web 依赖包
3. 建议在本地网络环境下使用
4. 任务执行期间保持浏览器页面打开
5. 配置文件仍使用 `config.yml`
6. 服务器默认监听所有网络接口（0.0.0.0），注意安全
7. 生产环境建议使用 Gunicorn 或 uWSGI 代替 Flask 开发服务器

## 🐛 问题反馈

如遇到问题，请在 GitHub Issues 提交，并附上：
- 浏览器控制台错误信息
- 后端日志文件
- 复现步骤

## 📄 开源协议

本项目遵循 GPL-3.0 开源协议
